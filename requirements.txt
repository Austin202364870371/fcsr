# Complete FCSR runtime: preprocessing, training, and evaluation.
numpy>=1.24,<3
PyYAML>=6.0,<7
tqdm>=4.66,<5

# Qwen training and model download.
# For CUDA, install the PyTorch build matching the host image first when necessary:
# https://pytorch.org/get-started/locally/
torch>=2.3,<2.7
transformers==4.51.3
peft==0.11.1
accelerate==0.31.0
safetensors>=0.4.3,<1
sentencepiece>=0.2,<1
huggingface-hub[cli]>=0.30,<1
